{
  "name": "iwacuhub-server",
  "version": "1.0.0",
  "description": "iWacuHub Server",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_OPTIONS=--max_old_space_size=4096 && react-scripts start",
    "build": "set NODE_OPTIONS=--max_old_space_size=4096 && react-scripts build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mysql2": "^3.6.0",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}